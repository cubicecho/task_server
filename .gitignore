node_modules
dist
# Typed documents generated from web/graphql/**/*.graphql. Regenerated by `npm run codegen`,
# which `typecheck`, `build` and the dev server all run on their own — see codegen.ts.
web/__generated__
# The embedded postgres and anything else written at runtime. Re-created on demand.
data
.env
*.local
# Claude Code's own scratch space — worktrees, session state. Not part of the app.
.claude
